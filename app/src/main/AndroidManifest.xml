<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/world"
        android:label="@string/app_name"
        android:roundIcon="@drawable/world"
        android:supportsRtl="true"
        android:theme="@style/Theme.BankRomanova"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity" android:label="Отделения и банкоматы"
            android:theme="@style/Theme"/>
        <activity android:name="com.example.bankromanova.ThirdActivity"
            android:label="31.12.2018      Покупка    Продажа"

            android:theme="@style/Theme"/>
    </application>

</manifest>